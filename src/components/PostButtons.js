import React from 'react';

const PostButtons = () => {
    return (
        <div>
            <button>Like</button>
            <button>comment</button>
        </div>
    );
};

export default PostButtons;